// Point this at wherever you deployed the BACKEND package (the SAME backend
// the mobile app's config.js points to — that's what keeps data in sync).
// Local testing (backend running via `node server.js` on your machine): leave as is.
// After deploying the backend (Render/Railway/etc.), replace with that URL, e.g.
//   window.API_BASE = "https://solar-doc-backend.onrender.com";
window.API_BASE = "http://localhost:3000";
